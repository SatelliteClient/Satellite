package com.github.satellite.ui.gui.guiEditor;

import net.minecraft.client.gui.GuiScreen;

public class GuiGuiEditor extends GuiScreen {

    int x, y, width, height;
    boolean allowResize, allowMove;

    public GuiGuiEditor(int x, int y, int width, int height, boolean allowResize, boolean allowMove) {

    }

    @Override
    public void drawScreen(int mouseX, int mouseY, float partialTicks) {

        super.drawScreen(mouseX, mouseY, partialTicks);
    }
}
